import { makeStyles } from '@material-ui/styles'

export const useListItemStyles = makeStyles(theme => ({
	avatar: {
		backgroundColor: '#546334'
	},
	link: {
		color: 'unset',
		textDecoration: 'none',
	}
}))